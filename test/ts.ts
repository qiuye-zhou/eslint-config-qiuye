let one = 1;

export default one;

JSON.stringify({ test: 'test' });

let a = 3;

const test = 'test1';

for (let index = 0; index <= 10; index += 1) {
    a = index;
}

Number.parseInt(test + a, 10);
